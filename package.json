{
  "name": "cp0-kit",
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "node scripts/build-grid.js",
    "test": "node scripts/test.js",
    "test:unit": "node --test tests/*.test.js"
  }
}
